# design-system-template（上游快照）

- **仓库**：https://github.com/arevalolance/design-system-template
- **用途**：本目录为导入时的 **只读参考快照**（`packages-ui/`），便于对照 diff、升级或恢复。
- **实际使用代码**：见 `src/components/starter/`（已改为 `@/` 路径并接入当前 Tailwind v4 + `globals.css`）。
